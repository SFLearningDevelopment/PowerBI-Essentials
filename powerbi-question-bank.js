/**
 * Power BI Essentials — Question Bank
 * SF Certified Power BI Essentials Practitioner exam.
 * Samples 40 questions weighted by module exam-weight + difficulty mix.
 *
 * Schema: window.PB_QUESTION_BANK = { m{N}: { s{M}: [{id,q,options,correctIndex,explanation,difficulty}] } }
 * Difficulty: 'easy' | 'medium' | 'hard'  |  IDs: 'pb-mN-sM-iX'
 * Module weights: m1:0.10  m2:0.20  m3:0.15  m4:0.25  m5:0.15  m6:0.15
 */
window.PB_QUESTION_BANK = {

// ══════════════════════════════════════════════════════
// MODULE 1 — Getting Started with Power BI  (10%)
// ══════════════════════════════════════════════════════
m1:{
  s1:[ // Ecosystem & Products
    {id:'pb-m1-s1-i1',difficulty:'easy',
     q:'Which Power BI product is used to build and edit reports with full authoring capabilities?',
     options:['Power BI Service','Power BI Desktop','Power BI Mobile','Power BI Report Server'],
     correctIndex:1,explanation:'Power BI Desktop is the free Windows application where you connect to data, transform it, build the data model, write DAX, and create report pages.'},
    {id:'pb-m1-s1-i2',difficulty:'easy',
     q:'What is the primary purpose of Power BI Service (app.powerbi.com)?',
     options:['To write DAX measures and calculated columns','To transform data using Power Query','To publish, share, and govern Power BI content in the cloud','To install and manage on-premises data gateways'],
     correctIndex:2,explanation:'Power BI Service is the cloud platform for publishing, sharing, collaborating on, and governing Power BI reports and dashboards.'},
    {id:'pb-m1-s1-i3',difficulty:'medium',
     q:'A colleague needs to view a published Power BI report on their iPhone. Which product should they install?',
     options:['Power BI Desktop','Power BI Embedded','Power BI Mobile','Power BI Report Builder'],
     correctIndex:2,explanation:'Power BI Mobile is the app (iOS, Android, Windows) for viewing and interacting with published reports on mobile devices.'},
    {id:'pb-m1-s1-i4',difficulty:'medium',
     q:'Which Power BI component allows organisations to host Power BI reports on their own on-premises infrastructure instead of the cloud?',
     options:['Power BI Embedded','Power BI Report Server','Power BI Desktop','Power BI Premium'],
     correctIndex:1,explanation:'Power BI Report Server is an on-premises reporting portal — a local version of Power BI Service for organisations that cannot or will not host data in the cloud.'}
  ],
  s2:[ // Desktop Interface
    {id:'pb-m1-s2-i1',difficulty:'easy',
     q:'In Power BI Desktop, which view shows a grid of all rows in a table so you can verify data and write calculated columns?',
     options:['Report View','Model View','Data View','Query Editor'],
     correctIndex:2,explanation:'Data View displays the rows of each table in a grid. It is used to verify data, check data types, and write calculated columns whose results appear row by row.'},
    {id:'pb-m1-s2-i2',difficulty:'easy',
     q:'In which Power BI Desktop view do you create and manage relationships between tables?',
     options:['Report View','Data View','Model View','Query View'],
     correctIndex:2,explanation:'Model View displays all tables as boxes with relationship connector lines. You drag columns to create relationships and double-click lines to edit them.'},
    {id:'pb-m1-s2-i3',difficulty:'medium',
     q:'Can you add, edit, or delete rows of data in Power BI Desktop\'s Data View?',
     options:['Yes — right-click any cell to edit it','Yes — but only if the table uses DirectQuery mode','No — Power BI is read-only from a data perspective; data changes happen in the source or Power Query','No — and you must use DAX Studio to modify data'],
     correctIndex:2,explanation:'Power BI cannot modify source data. Data View is read-only for browsing data. All data changes must happen in the source system or in Power Query during transformation.'},
    {id:'pb-m1-s2-i4',difficulty:'hard',
     q:'You open Power BI Desktop and see the Fields pane, Visualizations pane, and Format pane. Which view are you in?',
     options:['Model View','Data View','Report View','Query View'],
     correctIndex:2,explanation:'Report View shows the canvas with the Visualizations pane (chart picker and visual wells) and Format pane for styling. This is where you build the end-user report pages.'}
  ],
  s3:[ // Connecting to Data
    {id:'pb-m1-s3-i1',difficulty:'easy',
     q:'In Power BI Import mode, when is the data in the model updated from the source?',
     options:['On every visual interaction — data is always live','Only when manually triggered or on a scheduled refresh cycle','Automatically every 15 minutes by default','When the report is opened by an end user'],
     correctIndex:1,explanation:'Import mode copies data into the Vertipaq engine at load/refresh time. The model stays static until the next manual or scheduled refresh.'},
    {id:'pb-m1-s3-i2',difficulty:'easy',
     q:'Which connection mode sends a new query to the source database every time a user interacts with a visual?',
     options:['Import','DirectQuery','Live Connection','Composite'],
     correctIndex:1,explanation:'DirectQuery sends a query to the source on every visual interaction — no data is stored locally. This ensures always-current data but performance depends entirely on the source database.'},
    {id:'pb-m1-s3-i3',difficulty:'medium',
     q:'What is the key advantage of DirectQuery over Import mode?',
     options:['DirectQuery is faster because it uses the Vertipaq engine','DirectQuery always shows the most current data because it queries the source in real time','DirectQuery supports more DAX functions than Import mode','DirectQuery models have no size limits'],
     correctIndex:1,explanation:'DirectQuery\'s advantage is data freshness — every interaction queries the source so data is always current. Its disadvantage is performance dependency on the source system.'},
    {id:'pb-m1-s3-i4',difficulty:'hard',
     q:'A Power BI model connects to an Azure SQL database (Import mode) and a published Power BI dataset (Live Connection). What mode is this?',
     options:['Hybrid mode','DirectQuery mode','Composite model','Dual storage mode'],
     correctIndex:2,explanation:'Composite models combine Import, DirectQuery, and/or Live Connection sources in one model. This allows mixing local data with remote/cloud sources in a single report.'}
  ],
  s4:[ // Report View Data View Model View
    {id:'pb-m1-s4-i1',difficulty:'easy',
     q:'You want to add a new calculated column to the fact_sales table using a DAX formula. Which Power BI Desktop view do you switch to?',
     options:['Report View','Model View','Data View — Table tools → New Column','Query Editor'],
     correctIndex:2,explanation:'Calculated columns are written in Data View (or via the Modeling tab). After writing the DAX formula, the result appears as a new column visible in the data grid.'},
    {id:'pb-m1-s4-i2',difficulty:'medium',
     q:'You want to create a relationship between dim_customers and fact_sales. Which view do you navigate to?',
     options:['Report View — drag fields between visuals','Data View — drag column headers between tables','Model View — drag a column onto another table\'s column','Query Editor — merge the queries'],
     correctIndex:2,explanation:'Relationships are created and managed in Model View by dragging columns between table boxes or using the Manage Relationships dialog.'},
    {id:'pb-m1-s4-i3',difficulty:'medium',
     q:'In which Power BI Desktop view would you set a custom format for a date column (e.g. "YYYY-MMM")?',
     options:['Report View — Format pane','Model View — Column tools','Data View — Column tools in the ribbon','Query Editor — Transform tab'],
     correctIndex:2,explanation:'Column formatting (data types, format strings) is set in Data View using the Column tools tab or via Modeling. This affects how the column displays throughout the report.'},
    {id:'pb-m1-s4-i4',difficulty:'hard',
     q:'You are in Model View and see a yellow triangle icon on a relationship line. What does this indicate?',
     options:['The relationship is inactive','The relationship has a many-to-many cardinality warning or an ambiguous filter path','The table on one side is empty','The relationship uses an unsupported data type'],
     correctIndex:1,explanation:'A yellow triangle on a relationship in Model View indicates a potential ambiguity — often a many-to-many cardinality that Power BI did not expect, or an ambiguous cross-filter path that could produce incorrect results.'}
  ],
  s5:[ // Publishing to Power BI Service
    {id:'pb-m1-s5-i1',difficulty:'easy',
     q:'When you publish from Power BI Desktop to the Service, what two artefacts are created?',
     options:['A report and a dashboard','A dataset and a report','A workspace and a dataset','A dataflow and a report'],
     correctIndex:1,explanation:'Publishing creates: (1) a dataset — the data model with tables, relationships, and measures; and (2) a report — the visual layout. These are separate, independently managed objects in the Service.'},
    {id:'pb-m1-s5-i2',difficulty:'medium',
     q:'What is required to refresh an Import dataset built on a local SQL Server database from Power BI Service?',
     options:['A Power BI Premium licence','An On-Premises Data Gateway installed on a machine with access to the SQL Server','A DirectQuery connection instead of Import','Nothing — the Service can reach local databases directly'],
     correctIndex:1,explanation:'Power BI Service cannot reach on-premises databases directly. An On-Premises Data Gateway acts as a bridge — it runs locally, receives refresh requests from the Service, queries the local database, and sends data back.'},
    {id:'pb-m1-s5-i3',difficulty:'medium',
     q:'Can multiple Power BI reports be connected to one shared published dataset?',
     options:['No — each report must have its own dataset','Yes — a published dataset is an independent object that multiple reports can connect to','Yes — but only within the same workspace','No — a dataset can only serve the report published with it'],
     correctIndex:1,explanation:'A published dataset is a standalone, shareable object. Any authorised user can create a new report on top of an existing published dataset — avoiding data duplication and ensuring consistency.'},
    {id:'pb-m1-s5-i4',difficulty:'hard',
     q:'A Power BI dataset uses an Import connection to a CSV file stored on a local laptop. The report is published to Service. What happens when a scheduled refresh runs?',
     options:['Power BI Service refreshes successfully using the laptop\'s last uploaded version','The refresh fails — the Service cannot access local file systems without a gateway','The Service downloads the CSV from the laptop automatically','The report switches to DirectQuery mode to maintain freshness'],
     correctIndex:1,explanation:'Power BI Service cannot access local file systems. Without a gateway, any dataset pointing to a local file will fail to refresh. Move the file to SharePoint Online or OneDrive for gateway-free cloud refresh.'}
  ],
  s6:[ // Lab
    {id:'pb-m1-s6-i1',difficulty:'easy',
     q:'After loading sf-sales-orders.csv in Power BI Desktop, you switch to Data View. What can you verify there?',
     options:['The relationships between tables','The column names data types and row values of the loaded table','The visual layout and chart formatting','The DAX measures written in the model'],
     correctIndex:1,explanation:'Data View shows the columns and rows of each table — the correct place to verify column names, data types, and that data loaded correctly.'},
    {id:'pb-m1-s6-i2',difficulty:'medium',
     q:'You insert a Slicer visual with field = region. You select "North" in the slicer. What happens to a bar chart showing Revenue by Category on the same page?',
     options:['Nothing — slicers only affect the slicer visual itself','The bar chart filters to show only revenue from North region orders','The bar chart is highlighted but non-North data remains visible','The bar chart refreshes the data from the source system'],
     correctIndex:1,explanation:'By default, a slicer filters all visuals on the same page. Selecting "North" passes a filter context (region = North) to all other visuals including the bar chart.'},
    {id:'pb-m1-s6-i3',difficulty:'medium',
     q:'After publishing to Power BI Service and pinning a card visual to a Dashboard, what happens when the dataset is refreshed?',
     options:['The Dashboard tile updates automatically after each scheduled refresh','The Dashboard tile only updates when the user manually refreshes the browser','The Dashboard tile becomes outdated and must be re-pinned','The Dashboard tile shows data from the publish date permanently'],
     correctIndex:0,explanation:'Dashboard tiles in Power BI Service update automatically after each dataset refresh — they always show the latest data from the most recent refresh cycle.'}
  ]
},

// ══════════════════════════════════════════════════════
// MODULE 2 — Power Query & M Language  (20%)
// ══════════════════════════════════════════════════════
m2:{
  s1:[ // Power Query Overview
    {id:'pb-m2-s1-i1',difficulty:'easy',
     q:'What is Power Query in Power BI and when does it run?',
     options:['Power Query is the DAX formula engine — it runs on every visual interaction','Power Query is the ETL layer — it connects to sources and transforms data during dataset refresh','Power Query is the rendering engine that formats charts and tables','Power Query is a cloud service that transforms data in Azure before it reaches Power BI'],
     correctIndex:1,explanation:'Power Query is Power BI\'s ETL (Extract, Transform, Load) tool. It runs at refresh time to connect to data sources, apply transformation steps, and load clean data into the model.'},
    {id:'pb-m2-s1-i2',difficulty:'easy',
     q:'Where do you access Power Query in Power BI Desktop?',
     options:['View → Query Editor','Modeling → Power Query','Home → Transform Data','Data → Power Query Editor'],
     correctIndex:2,explanation:'In Power BI Desktop: Home tab → Transform Data opens the Power Query Editor window where all data transformation steps are configured.'},
    {id:'pb-m2-s1-i3',difficulty:'medium',
     q:'What does "Close & Apply" do in the Power Query Editor?',
     options:['Saves the M code to a local file without loading data','Closes Power Query Editor and applies all pending transformations — loading the transformed data into the Power BI model','Publishes the transformations to Power BI Service','Clears all transformation steps and resets to the original data'],
     correctIndex:1,explanation:'"Close & Apply" exits the Power Query Editor and executes all defined transformation steps, loading the resulting data into the Power BI data model.'},
    {id:'pb-m2-s1-i4',difficulty:'hard',
     q:'Power Query transformation steps are shown in the "Applied Steps" pane. What is the significance of step order?',
     options:['Steps run in parallel — order does not matter','Steps run sequentially — each step receives the output of the previous step as its input','Steps run in reverse alphabetical order','Only the final step is applied — intermediate steps are for documentation only'],
     correctIndex:1,explanation:'Power Query applies steps sequentially. Each step takes the output of the preceding step as its input. Reordering steps can change results — deleting or moving an early step may break all dependent steps.'}
  ],
  s2:[ // Applied Steps & Query Folding
    {id:'pb-m2-s2-i1',difficulty:'easy',
     q:'What is "query folding" in Power Query?',
     options:['The process of collapsing multiple queries into one for performance','The ability of Power Query to translate transformation steps into native source queries (e.g. SQL) that run on the source database rather than in Power BI','The compression of data before it is loaded into the Vertipaq engine','The automatic removal of duplicate transformation steps'],
     correctIndex:1,explanation:'Query folding means Power Query can push transformation logic back to the source as a native query (SQL, OData, etc.) — letting the source database do the heavy lifting instead of loading raw data into Power BI first.'},
    {id:'pb-m2-s2-i2',difficulty:'medium',
     q:'You connect Power Query to a SQL Server table. Which transformation is most likely to prevent query folding?',
     options:['Remove Duplicates','Filter Rows — Revenue > 500','Sort Rows — Ascending by order_date','Add a custom column using a complex M function like Table.NestedJoin'],
     correctIndex:3,explanation:'Complex M functions that have no SQL equivalent (like Table.NestedJoin) cannot be folded to SQL. Once a non-foldable step is added, all subsequent steps also lose folding — Power Query downloads the data and processes locally.'},
    {id:'pb-m2-s2-i3',difficulty:'medium',
     q:'How can you verify whether a Power Query step is being folded to the source?',
     options:['Right-click the step in Applied Steps → "View Native Query" (if greyed out folding is not happening for this step)','Open DAX Studio and run a server timing trace','Check the Power Query Diagnostics report in the Home tab','Query folding cannot be verified in Power BI Desktop'],
     correctIndex:0,explanation:'Right-click any Applied Step → "View Native Query". If the option is clickable it shows the SQL/OData being sent to the source (folding is active). If it is greyed out folding has broken at this step.'},
    {id:'pb-m2-s2-i4',difficulty:'hard',
     q:'A Power Query transformation is NOT being folded to SQL Server. What is the performance consequence?',
     options:['No performance impact — local and server processing are equally fast','Power BI downloads the full table from SQL Server before applying the transformation locally — increasing refresh time and network load','The transformation is skipped to maintain performance','Power BI automatically rewrites the M code to enable folding'],
     correctIndex:1,explanation:'Without folding, Power Query downloads the entire unfiltered/untransformed dataset from the source and applies the transformation steps locally. For large tables this dramatically increases refresh time and network traffic.'}
  ],
  s3:[ // Connecting to Data Sources in Power Query
    {id:'pb-m2-s3-i1',difficulty:'easy',
     q:'In Power Query, what does "Use First Row as Headers" do?',
     options:['Moves all row values up by one row','Promotes the first data row to become column headers and removes it from the data rows','Removes the first row of data permanently','Sorts rows alphabetically using the first column as the key'],
     correctIndex:1,explanation:'"Use First Row as Headers" is a one-click transformation that takes the first row (which contains column names) and sets it as the header row for the table.'},
    {id:'pb-m2-s3-i2',difficulty:'medium',
     q:'You connect to twelve monthly CSV files (sales_jan.csv, sales_feb.csv ... sales_dec.csv) all with identical columns. How do you load all twelve into one query in Power Query?',
     options:['Manually connect to each file then union them in Power Query','Connect to the folder containing the files using "Get Data → Folder" and combine them into one table with one operation','Connect to one file and use the Duplicate function twelve times','Power BI cannot combine multiple CSV files — use Tableau Prep instead'],
     correctIndex:1,explanation:'Get Data → Folder → navigate to the folder containing the CSVs → Power Query offers to "Combine" them automatically, stacking all matching-schema files into one query.'},
    {id:'pb-m2-s3-i3',difficulty:'medium',
     q:'After connecting a CSV file to Power Query, the date column is showing as data type "Text" (ABC icon). What must you do?',
     options:['Delete and reload the CSV with a different encoding','Click the ABC icon on the column header → select Date from the list to change the data type','Right-click the column → Transform → Date and use Parse','Nothing — Power BI will automatically convert text to date when needed in DAX'],
     correctIndex:1,explanation:'Click the data type icon (ABC, 123, calendar icon) on the column header → select the correct type (Date). This adds a "Changed Type" step. Dates stored as text cannot be used in time intelligence functions.'},
    {id:'pb-m2-s3-i4',difficulty:'hard',
     q:'You connect a CSV where all data appears in one column with long strings like "1001,2024-01-15,Electronics,450.00" instead of separate columns. What is the most likely cause and fix?',
     options:['The file has no data — re-export from the source','The CSV uses a semicolon or pipe as the delimiter instead of a comma — fix the delimiter in the Source step','Power Query does not support CSV files with more than 3 columns','The file must be converted to Excel format before Power Query can parse it'],
     correctIndex:1,explanation:'If the delimiter doesn\'t match Power Query\'s assumption (comma by default), all values appear in one column. Fix it in the Source step: gear icon next to the "Source" step → change the Delimiter setting.'}
  ],
  s4:[ // Transformations
    {id:'pb-m2-s4-i1',difficulty:'easy',
     q:'You need to split a "FirstName LastName" column into two separate columns in Power Query. Which transformation should you use?',
     options:['Add Column → Custom Column','Transform → Split Column → By Delimiter (space)','Transform → Pivot Column','Home → Group By'],
     correctIndex:1,explanation:'Transform → Split Column → By Delimiter → specify space as the delimiter. Power Query splits the column into two (or more) columns at each space.'},
    {id:'pb-m2-s4-i2',difficulty:'easy',
     q:'What does "Remove Duplicates" do when applied to a column in Power Query?',
     options:['Removes all rows where the selected column is null','Removes rows where the selected column value appears more than once — keeping only the first occurrence','Removes columns that contain only duplicate values','Deduplicates entire rows based on all columns simultaneously'],
     correctIndex:1,explanation:'"Remove Duplicates" on a column keeps only the first row for each unique value in that column — removing all subsequent rows with the same value. Applied to all columns it deduplicates full rows.'},
    {id:'pb-m2-s4-i3',difficulty:'medium',
     q:'What is the Pivot Column transformation used for in Power Query?',
     options:['To rotate the entire query table 90 degrees for display','To convert rows into columns — taking the unique values of one column as new column headers and aggregating another column\'s values into the cells','To add a calculated column based on other columns','To combine two queries side by side'],
     correctIndex:1,explanation:'Pivot Column converts unique values in a selected column into new column headers. Example: a "month" column with values Jan/Feb/Mar becomes three new columns, with revenue values aggregated into the cells.'},
    {id:'pb-m2-s4-i4',difficulty:'hard',
     q:'You need to replace "N/A" text values in a revenue column with 0. Which Power Query approach is correct?',
     options:['Transform → Replace Values → find "N/A" → replace with "0" then change data type to Decimal Number','Add a conditional column with IF revenue = "N/A" THEN 0 ELSE revenue','Home → Remove Rows → Remove Errors then fill null values with 0','DAX IF([revenue]="N/A",0,[revenue]) in a calculated column is the correct approach'],
     correctIndex:0,explanation:'Transform → Replace Values replaces specific text values before type conversion. Replace "N/A" with "0" (text) first, then change the column type to Decimal Number. The order matters — type conversion before replacement would produce errors.'}
  ],
  s5:[ // Merging & Appending Queries
    {id:'pb-m2-s5-i1',difficulty:'easy',
     q:'In Power Query, what is the difference between "Merge Queries" and "Append Queries"?',
     options:['Merge combines tables horizontally (adds columns from a second table via key matching); Append combines tables vertically (stacks rows from multiple tables)','Merge stacks rows; Append adds columns','Both operations do the same thing — just different names','Merge is for Excel sources; Append is for SQL sources'],
     correctIndex:0,explanation:'Merge = horizontal join (adds columns from a related table based on key matching — like SQL JOIN). Append = vertical union (stacks rows from tables with the same schema — like SQL UNION ALL).'},
    {id:'pb-m2-s5-i2',difficulty:'medium',
     q:'You use a Left Outer Merge to join fact_sales to dim_customers on customer_id. Some orders have a customer_id with no match in dim_customers. What do those rows look like in the result?',
     options:['Those rows are removed from the result','Those rows remain but dim_customers columns show null for all unmatched values','Those rows cause a merge error and the query fails','Those rows are moved to a separate error table in Power Query'],
     correctIndex:1,explanation:'Left Outer Merge keeps all rows from the left table (fact_sales). For orders with no matching customer, the dim_customers columns (name, region, etc.) show null — the order row itself is preserved.'},
    {id:'pb-m2-s5-i3',difficulty:'medium',
     q:'After merging two queries in Power Query, you see a column containing "Table" objects instead of values. What step must you perform?',
     options:['Change the column data type to Text','Click the expand icon (two-headed arrow) on the column header to expand the nested table and select which columns to include','Right-click the column → Drill Down to extract the nested values','The merge failed — delete and re-merge the queries'],
     correctIndex:1,explanation:'When queries are merged, the matched rows from the right table are stored as nested Table objects in a new column. Click the expand icon on that column header to select which columns from the right table to bring into the result.'},
    {id:'pb-m2-s5-i4',difficulty:'hard',
     q:'After an Append operation combining Q1 and Q2 sales, the result shows twice as many columns as expected — some columns appear twice with slightly different names. What is the most likely cause?',
     options:['The append created a cross-join instead of a union','Column names in the two queries are not exactly identical — Power Query creates separate columns instead of matching them','Append only works with tables that have fewer than 10 columns','The data types of the columns are different in the two source queries'],
     correctIndex:1,explanation:'Append matches columns by name exactly (case-insensitive). If Q1 has "Revenue" and Q2 has "revenue" or "Rev" they appear as separate columns with nulls in alternate rows. Standardize column names in both queries before appending.'}
  ],
  s6:[ // M Language Introduction
    {id:'pb-m2-s6-i1',difficulty:'easy',
     q:'What language does Power Query use behind the scenes for all transformations?',
     options:['SQL','Python','M (Power Query Formula Language)','DAX'],
     correctIndex:2,explanation:'Every transformation in Power Query generates M code. M is a functional, case-sensitive language. You can view and edit it via the Advanced Editor (Home → Advanced Editor).'},
    {id:'pb-m2-s6-i2',difficulty:'medium',
     q:'In M, what does the "let…in" construct do?',
     options:['let declares a variable; in executes a SQL statement against the source','let defines named transformation steps sequentially; in specifies which step\'s result is the query\'s final output','let initialises the Power Query connection; in specifies the table to load','let and in are identical — either keyword can be used anywhere'],
     correctIndex:1,explanation:'The let…in structure defines step names and their expressions (let), then specifies which step produces the final output table (in). Each step name references the previous step\'s result.'},
    {id:'pb-m2-s6-i3',difficulty:'medium',
     q:'You need to add a "full_name" column that concatenates first_name and last_name with a space. Which M expression is correct?',
     options:['Table.AddColumn(prev, "full_name", each [first_name] & " " & [last_name])','Table.NewColumn("full_name", [first_name] + [last_name])','AddColumn(prev, "full_name", [first_name] & [last_name])','Column.Concatenate([first_name], " ", [last_name])'],
     correctIndex:0,explanation:'Table.AddColumn takes the previous step, a new column name, and a row-level function (each … ) using & to concatenate text values. Square brackets reference column values for the current row.'},
    {id:'pb-m2-s6-i4',difficulty:'hard',
     q:'Why is M described as a "lazy" functional language?',
     options:['M is slow and should be replaced with Python for complex transformations','M only evaluates expressions when their results are actually needed — allowing Power Query to optimise which steps are actually executed','M does not support loops or recursion so it avoids complex computations','M evaluates all steps eagerly at query definition time not at execution time'],
     correctIndex:1,explanation:'M is lazy (deferred evaluation) — expressions are only evaluated when their output is required. This is what enables query folding: Power Query can determine what the source needs to provide without executing every intermediate step locally.'}
  ],
  s7:[ // Lab
    {id:'pb-m2-s7-i1',difficulty:'easy',
     q:'In the Power Query lab you rename a query from "sf-sales-orders" to "fact_sales". Where does this renamed query appear?',
     options:['In the Power BI Service workspace only','As the table name in the Power BI Desktop data model (Fields pane and relationships)','Only in the Power Query Editor pane — not visible in the model','In the DAX formula bar as the default table name for all new measures'],
     correctIndex:1,explanation:'Query names in Power Query Editor become table names in the Power BI data model. Renaming to "fact_sales" means the table appears as fact_sales in Model View, Data View, and the Fields pane.'},
    {id:'pb-m2-s7-i2',difficulty:'medium',
     q:'You use Home → Group By in Power Query to aggregate sales by region. After clicking OK you notice the resulting table has one row per region with a "Sum of Revenue" column. What happened to all the individual order rows?',
     options:['They were moved to a hidden table in the model','They were removed — Group By aggregates all rows matching the group key into a single summary row','They are still present in a sub-table accessible via the expand icon','They are stored in a separate query named "Original Rows"'],
     correctIndex:1,explanation:'Group By aggregates rows — all individual order rows for "North" become one row with the summed revenue. The original row-level detail is lost in this query unless you preserve it in a separate query first.'},
    {id:'pb-m2-s7-i3',difficulty:'hard',
     q:'You write this M step: Table.SelectRows(prev, each [revenue] > 500). What does this do?',
     options:['Selects (keeps) only the first 500 rows','Removes rows where revenue is greater than 500 — keeping only small orders','Keeps only rows where the revenue column value is greater than 500','Selects the revenue column and returns its first 500 values'],
     correctIndex:2,explanation:'Table.SelectRows filters rows based on a condition. "each [revenue] > 500" is a row-level function that returns true for rows where revenue exceeds 500 — those rows are kept. Rows with revenue ≤ 500 are removed.'}
  ]
},

// ══════════════════════════════════════════════════════
// MODULE 3 — Star Schema & Data Modeling  (15%)
// ══════════════════════════════════════════════════════
m3:{
  s1:[ // Why Modeling Matters
    {id:'pb-m3-s1-i1',difficulty:'easy',
     q:'What is the name of Power BI\'s in-memory analytical engine that processes data model queries?',
     options:['Vertipaq','PolyBase','xStore','Hyper'],
     correctIndex:0,explanation:'Vertipaq (also called xVelocity) is Power BI\'s columnar in-memory engine. It compresses data by column and enables extremely fast aggregate queries over millions of rows.'},
    {id:'pb-m3-s1-i2',difficulty:'easy',
     q:'Why does columnar storage (Vertipaq) benefit analytical queries?',
     options:['Columnar storage writes data faster than row-based storage','Analytical queries aggregate one or a few columns — columnar storage reads only those columns skipping all others and reducing I/O dramatically','Columnar storage supports ACID transactions required by DAX','Columnar storage compresses better only for text columns not for numbers'],
     correctIndex:1,explanation:'A SUM(revenue) query reads only the revenue column. With columnar storage all revenue values are stored adjacently — the engine reads one column\'s worth of data and skips all other columns entirely.'},
    {id:'pb-m3-s1-i3',difficulty:'medium',
     q:'Name two problems with using a flat denormalized table as the sole table in a multi-table Power BI model.',
     options:['Flat tables cannot be imported — they require DirectQuery','Repeated text values reduce compression efficiency; relationships between flat tables can be ambiguous leading to incorrect cross-filtering','Flat tables break all time intelligence DAX functions','Flat tables are not supported after Power BI Desktop version 2024'],
     correctIndex:1,explanation:'Repeated text (e.g. "Electronics" across 50,000 rows) compresses less efficiently than storing it once in a dimension table. Additionally, many-to-many joins between flat tables create ambiguous filter paths causing incorrect cross-filter results.'},
    {id:'pb-m3-s1-i4',difficulty:'hard',
     q:'Vertipaq compresses a column containing the values "North", "South", "East", "West" repeated across 2 million rows. Approximately how many distinct values does Vertipaq store internally for this column?',
     options:['2 million — one per row','4 — Vertipaq stores only the distinct values plus a compact index mapping each row to a value','The full column is not compressed — text columns are stored verbatim','16 — Vertipaq stores 4 values × 4 bytes encoding'],
     correctIndex:1,explanation:'Vertipaq stores only the 4 distinct string values in a dictionary, then for each row stores a tiny integer index (e.g. 0=North, 1=South, 2=East, 3=West). 2 million integers replace 2 million copies of the text — massive compression.'}
  ],
  s2:[ // Star Schema Design
    {id:'pb-m3-s2-i1',difficulty:'easy',
     q:'In a star schema, what type of table sits at the center of the model?',
     options:['A dimension table','A fact table','A bridge table','A lookup table'],
     correctIndex:1,explanation:'The fact table sits at the center of a star schema. It contains business event records (orders, transactions) with foreign keys pointing outward to surrounding dimension tables.'},
    {id:'pb-m3-s2-i2',difficulty:'easy',
     q:'What does the "one grain" principle mean for a fact table?',
     options:['A fact table can have only one numeric measure column','Every row in the fact table must represent the same type of business event at the same level of detail','A fact table can only relate to one dimension table','The fact table must have exactly one primary key column'],
     correctIndex:1,explanation:'One grain: every row in the fact table represents one instance of the same event type at the same granularity. Mixing grains (some rows = orders, some rows = order lines) breaks aggregation logic.'},
    {id:'pb-m3-s2-i3',difficulty:'medium',
     q:'Why should dimension tables be "wide and flat" rather than normalized into separate tables?',
     options:['Normalized dimensions are not supported by Power BI','Flat wide dimensions require fewer joins at query time — improving DAX performance and simplifying the model','Flat dimensions automatically enable bidirectional cross-filtering','Wide dimensions are required for RLS to function correctly'],
     correctIndex:1,explanation:'In traditional databases normalization reduces storage redundancy. In Vertipaq storage is cheap and compression handles repetition. Flat wide dimensions (all attributes in one table) means fewer relationships, simpler DAX, and faster query resolution.'},
    {id:'pb-m3-s2-i4',difficulty:'hard',
     q:'A date dimension table must have a contiguous range of dates with no gaps. Why?',
     options:['Power BI enforces this rule and will reject tables with gaps when marked as Date Table','DAX time intelligence functions (TOTALYTD, SAMEPERIODLASTYEAR) traverse the date table day by day — gaps cause incorrect period-to-date calculations','Gaps in the date table break the visual hierarchy for drill-down','A contiguous date range is required to enable bidirectional cross-filtering on the date dimension'],
     correctIndex:1,explanation:'Time intelligence functions step through consecutive dates. A gap (e.g. no rows for Feb 28-29 in a leap year) causes those days to be skipped or for period boundaries to be calculated incorrectly.'}
  ],
  s3:[ // Fact and Dimension Tables
    {id:'pb-m3-s3-i1',difficulty:'easy',
     q:'Which of these columns belongs in a fact table, not a dimension table?',
     options:['customer_email','product_category','revenue','region_name'],
     correctIndex:2,explanation:'Revenue is an additive numeric measure — it belongs in the fact table to be aggregated (summed, averaged). Descriptive attributes (email, category, region name) belong in dimension tables.'},
    {id:'pb-m3-s3-i2',difficulty:'medium',
     q:'You need a date dimension in your Power BI model. Which DAX function creates a single-column table with one row per calendar day?',
     options:['DATESBETWEEN()','CALENDAR(start_date, end_date)','GENERATEDATE()','DATEADD()'],
     correctIndex:1,explanation:'CALENDAR(start_date, end_date) returns a table with one column ("Date") containing one row for every calendar day between the two dates inclusive. Use ADDCOLUMNS() to add Year, Month, Quarter etc.'},
    {id:'pb-m3-s3-i3',difficulty:'medium',
     q:'After creating your dim_date table, what additional step must you perform for time intelligence DAX functions to work?',
     options:['Set all date columns to a "Date" data type','Right-click dim_date in Model View → Mark as Date Table → select the date column','Create a relationship from dim_date to every fact table','Add a calculated column "DateKey" formatted as YYYYMMDD integer'],
     correctIndex:1,explanation:'Power BI requires you to explicitly mark a table as the Date Table. This tells the engine which table and which column to use for time intelligence calculations. Without this marking TOTALYTD and similar functions produce errors or incorrect results.'},
    {id:'pb-m3-s3-i4',difficulty:'hard',
     q:'Why should foreign key columns (customer_id, product_id) in a fact table be hidden from the report view?',
     options:['Foreign keys must be hidden for RLS to work correctly','Integer IDs are meaningless to report users who should instead use the related name/label from the dimension table; hiding keeps the Fields pane clean and prevents incorrect use','Hidden columns are excluded from Vertipaq storage reducing model size','Foreign key columns cannot be used in DAX measures unless hidden'],
     correctIndex:1,explanation:'Hiding foreign keys prevents report authors from accidentally placing a meaningless ID (customer_id = 42) in a visual instead of the name (customer_name = "Acme Corp"). The ID is still used by the relationship engine — hiding from report view does not affect relationship functionality.'}
  ],
  s4:[ // Creating Relationships
    {id:'pb-m3-s4-i1',difficulty:'easy',
     q:'In Power BI Desktop Model View, how do you create a relationship between two tables?',
     options:['Write a DAX RELATEDTABLE() expression that links the tables','Drag a column from one table and drop it onto the matching column of another table in Model View','Go to File → Options → Relationships → Add New','Use the USERELATIONSHIP() function in a calculated column'],
     correctIndex:1,explanation:'In Model View drag a column from one table and drop it onto the corresponding column in another table. Power BI draws the relationship connector and auto-detects cardinality.'},
    {id:'pb-m3-s4-i2',difficulty:'easy',
     q:'What is the difference between an active and an inactive relationship?',
     options:['Active relationships are bidirectional; inactive relationships are single-direction','Only one active relationship can exist between two tables — it is used by default; inactive relationships (dashed lines) are used only when explicitly activated in DAX via USERELATIONSHIP()','Active relationships use Import mode; inactive use DirectQuery mode','Inactive relationships are disabled permanently and cannot be used in any calculation'],
     correctIndex:1,explanation:'Power BI enforces one active relationship per table pair. Additional relationships are marked inactive (dashed lines). They can be activated selectively in a DAX measure using USERELATIONSHIP(col1, col2).'},
    {id:'pb-m3-s4-i3',difficulty:'medium',
     q:'You have two date columns in fact_sales: order_date and ship_date. You create relationships from both to dim_date. What is the result?',
     options:['Both relationships are active simultaneously','One relationship (the first created or the one Power BI selects) is active; the second becomes inactive — shown as a dashed line','Power BI refuses to create the second relationship because two relationships between the same tables are not allowed','Both relationships become inactive and must be activated in DAX'],
     correctIndex:1,explanation:'Only one active relationship can exist between fact_sales and dim_date at a time. The second relationship becomes inactive (dashed line). Use USERELATIONSHIP(fact_sales[ship_date], dim_date[date]) inside a measure to route through the inactive relationship.'},
    {id:'pb-m3-s4-i4',difficulty:'hard',
     q:'Which DAX function activates an inactive relationship for the duration of a specific measure calculation?',
     options:['RELATEDTABLE()','CROSSFILTER()','USERELATIONSHIP(column1, column2)','ACTIVATERELATIONSHIP()'],
     correctIndex:2,explanation:'USERELATIONSHIP(col1, col2) is a CALCULATE modifier that activates the specified inactive relationship during the measure\'s evaluation. It does not permanently change the model — only affects the measure where it is used.'}
  ],
  s5:[ // Cardinality & Cross-Filter Direction
    {id:'pb-m3-s5-i1',difficulty:'easy',
     q:'In a standard star schema, what is the correct cardinality for a dimension-to-fact relationship?',
     options:['One-to-One (1:1)','Many-to-One (*:1) — many fact rows per one dimension row','Many-to-Many (*:*)','One-to-Many (1:*) from fact to dimension'],
     correctIndex:1,explanation:'The standard star schema relationship is Many-to-One from fact to dimension: many order rows in fact_sales relate to one customer row in dim_customers. Power BI displays this as *:1 on the fact side.'},
    {id:'pb-m3-s5-i2',difficulty:'medium',
     q:'What does "Single" cross-filter direction mean on a dimension-to-fact relationship?',
     options:['Filters flow from the fact table to the dimension table only','Filters flow from the dimension table to the fact table only — the standard direction for star schema cross-filtering','Both tables filter each other simultaneously','Filters apply only to single-column selections not multi-select'],
     correctIndex:1,explanation:'Single cross-filter direction flows from the "one" side (dimension) to the "many" side (fact). Selecting "Electronics" in dim_product filters fact_sales to show only Electronics rows — the standard star schema behaviour.'},
    {id:'pb-m3-s5-i3',difficulty:'medium',
     q:'When is bidirectional cross-filter direction appropriate in a Power BI model?',
     options:['Always — bidirectional should be the default for all relationships','For bridge tables in many-to-many scenarios where filters must flow in both directions to produce correct results','When the fact table has more rows than the dimension table','When using DirectQuery mode — bidirectional is required for query folding'],
     correctIndex:1,explanation:'Bidirectional filtering is appropriate for bridge tables (e.g. a product-tag mapping table where products have multiple tags and tags apply to multiple products). For standard dimension-to-fact relationships use Single direction.'},
    {id:'pb-m3-s5-i4',difficulty:'hard',
     q:'Power BI automatically creates a Many-to-Many (*:*) relationship when you drag two columns together. What does this suggest about the data?',
     options:['The two tables are identical and should be merged','Neither column contains unique values — the join key has duplicates on both sides indicating a missing or incorrect primary key in the dimension table','Many-to-many is the correct cardinality for all fact-to-fact relationships','The columns have incompatible data types that Power BI is trying to reconcile'],
     correctIndex:1,explanation:'Power BI creates M:M when neither column is unique. In a star schema the dimension table\'s join key must be unique (one row per customer). If Power BI auto-creates M:M it usually means the dimension table has duplicate primary keys — a data quality issue to fix in Power Query.'}
  ],
  s6:[ // Model Optimization
    {id:'pb-m3-s6-i1',difficulty:'easy',
     q:'What is the effect of hiding a column from the report view in Power BI Desktop?',
     options:['The column is deleted from the data model permanently','The column is excluded from the Fields pane so report authors cannot use it directly but it remains in the model for relationships and DAX','The column is removed from Vertipaq storage reducing model size permanently','Hiding a column prevents it from being used in any DAX calculation'],
     correctIndex:1,explanation:'Hidden columns remain fully functional in the model — they are used by relationships, and DAX can reference them. They simply do not appear in the Fields pane for report authors. This keeps the authoring experience clean.'},
    {id:'pb-m3-s6-i2',difficulty:'medium',
     q:'Why should the "Auto date/time" option be disabled in production Power BI models?',
     options:['Auto date/time prevents time intelligence DAX functions from working','Auto date/time creates a hidden date table for every date column in the model — inflating model size with tables that may never be used','Auto date/time forces all reports to use a US date format','Auto date/time requires a Power BI Premium licence to function correctly'],
     correctIndex:1,explanation:'Auto date/time creates a hidden dim_date-like table for every single date column. A model with 5 date columns gets 5 hidden tables. Most of these are unused. Disable it and create one explicit dim_date table instead.'},
    {id:'pb-m3-s6-i3',difficulty:'medium',
     q:'In terms of model memory usage, what is the trade-off between a calculated column and a measure?',
     options:['Calculated columns use no memory; measures use memory for each result','A calculated column stores a value for every row in its table (consuming memory); a measure stores nothing — it computes a single result on demand','Both calculated columns and measures store the same amount of data','Measures use more memory because they recalculate on every visual interaction'],
     correctIndex:1,explanation:'Calculated columns: memory = (row count × column value size). A million-row table with one calculated column stores 1 million values. Measures: memory = 0 stored values. This is why measures are strongly preferred over calculated columns for non-filtering use cases.'},
    {id:'pb-m3-s6-i4',difficulty:'hard',
     q:'Which of the following column types would compress LEAST efficiently in Vertipaq?',
     options:['A "region" column with 4 unique values repeated across 1 million rows','A "customer_email" column with 1 million unique email addresses','An "is_active" boolean column with only TRUE or FALSE values','A "year" column with values 2020 2021 2022 2023 2024'],
     correctIndex:1,explanation:'Vertipaq compression is most effective on low-cardinality columns. A million unique email addresses have cardinality = 1 million — effectively no compression is possible. Each value must be stored individually. This is why high-cardinality text columns (GUIDs, emails, free text) should be hidden or excluded from the model when not needed.'}
  ],
  s7:[ // Lab
    {id:'pb-m3-s7-i1',difficulty:'easy',
     q:'In the Lab 3 star schema, after dragging fact_sales[customer_id] onto dim_customers[customer_id] in Model View, what should you verify in the relationship dialog?',
     options:['That the relationship uses bidirectional cross-filter direction','That the cardinality shows Many to One (*:1) and cross-filter direction is Single','That an inactive relationship was created instead of an active one','That the data types of both columns are changed to Text'],
     correctIndex:1,explanation:'For a standard dimension-to-fact relationship verify: cardinality = Many to One (*:1 from fact side), cross-filter direction = Single (filters flow from dim_customers to fact_sales). This is the correct star schema setting.'},
    {id:'pb-m3-s7-i2',difficulty:'medium',
     q:'In the lab you mark dim_date as the Date Table. What would happen if you skipped this step and then wrote TOTALYTD([Total Revenue], dim_date[date])?',
     options:['TOTALYTD would work identically with or without marking the Date Table','TOTALYTD would return an error or incorrect results because it requires a properly marked Date Table to know the year boundaries','TOTALYTD would switch to using the auto date/time table instead','TOTALYTD would compute the running total from the beginning of all available data not from January 1'],
     correctIndex:1,explanation:'Time intelligence functions require a marked Date Table. Without the marking the function cannot determine fiscal or calendar year boundaries correctly — it may return errors or silently produce wrong values.'},
    {id:'pb-m3-s7-i3',difficulty:'hard',
     q:'You complete the star schema with dim_date and dim_customers both related to fact_sales. A region slicer (from dim_customers) is on the report page. You select "North" in the slicer. How does the filter reach the revenue values in fact_sales?',
     options:['The slicer filters fact_sales directly because revenue is in fact_sales','The filter travels from dim_customers through the active 1:M relationship to fact_sales — only fact_sales rows with matching customer_id records where region=North are visible','The filter is passed via DAX RELATED() function automatically','The slicer broadcasts to all tables simultaneously regardless of relationships'],
     correctIndex:1,explanation:'With Single cross-filter direction on the dim_customers → fact_sales relationship filters propagate from the "one" side (dim_customers) to the "many" side (fact_sales). Selecting North in the slicer filters dim_customers to North rows then the relationship propagates that filter to fact_sales matching rows.'}
  ]
},

// ══════════════════════════════════════════════════════
// MODULE 4 — DAX: Measures, Columns & Context  (25%)
// ══════════════════════════════════════════════════════
m4:{
  s1:[ // Introduction to DAX
    {id:'pb-m4-s1-i1',difficulty:'easy',
     q:'What does DAX stand for?',
     options:['Data Analysis eXpressions','Dynamic Aggregation eXecutor','Database Analytics eXchange','Data Aggregation and eXtraction'],
     correctIndex:0,explanation:'DAX stands for Data Analysis Expressions — the formula language used in Power BI, Power Pivot, and Azure Analysis Services for creating calculations on tabular data models.'},
    {id:'pb-m4-s1-i2',difficulty:'easy',
     q:'How do you reference the revenue column of the fact_sales table inside a DAX measure?',
     options:['fact_sales.revenue','[revenue]','fact_sales[revenue]','revenue(fact_sales)'],
     correctIndex:2,explanation:'In DAX, column references use the format TableName[ColumnName]. Inside a measure, always include the table name to avoid ambiguity: fact_sales[revenue].'},
    {id:'pb-m4-s1-i3',difficulty:'medium',
     q:'Why does the same DAX measure return different values in different rows of a matrix visual?',
     options:['The measure contains a random number generator','The matrix creates a different filter context for each cell — the measure recalculates based on which row and column intersection it is computing for','Different rows use different versions of the dataset','Power BI applies row-level security differently to each matrix row'],
     correctIndex:1,explanation:'DAX measures are context-sensitive. Each cell in a matrix has a filter context defined by its row and column headers. The measure runs independently for each cell with that cell\'s filter context applied — producing different results per cell.'},
    {id:'pb-m4-s1-i4',difficulty:'hard',
     q:'What is the key conceptual difference between a DAX formula and an Excel formula?',
     options:['DAX formulas are faster because they run on a server','Excel formulas reference specific cells (=A1+B1); DAX formulas operate on entire columns and tables in the current filter context — the "current row" is replaced by "currently visible rows"','Excel formulas support more functions than DAX','DAX formulas cannot use text strings while Excel formulas can'],
     correctIndex:1,explanation:'Excel: you reference specific cells and the formula returns one fixed value. DAX: you reference columns/tables and the measure returns a value aggregated over whatever rows are currently visible given the filter context — which changes dynamically as users interact.'}
  ],
  s2:[ // Measures vs Calculated Columns
    {id:'pb-m4-s2-i1',difficulty:'easy',
     q:'A calculated column vs a measure: which is stored in the model and which is computed on demand?',
     options:['Both are computed on demand — neither is stored','Measures are stored; calculated columns are computed on demand','Calculated columns are stored for every row at refresh time; measures are computed on demand at query time','Both are stored — measures cache results for performance'],
     correctIndex:2,explanation:'Calculated columns: computed at refresh and stored in Vertipaq for every row (consumes memory). Measures: no storage — computed fresh at query time for each visual interaction based on filter context.'},
    {id:'pb-m4-s2-i2',difficulty:'easy',
     q:'You need to use "customer_region" as a slicer on a dashboard. The region field is only in dim_customers but you need it row-by-row in fact_sales for segmentation. Should you create a calculated column or a measure?',
     options:['A measure: Customer Region = RELATED(dim_customers[region])','A calculated column in fact_sales: Customer Region = RELATED(dim_customers[region])','A calculated column in dim_customers — not in fact_sales','Neither — add region directly to the slicer from dim_customers without adding it to fact_sales'],
     correctIndex:1,explanation:'To slice by region using fact_sales rows, you need region available as a column in fact_sales. Calculated columns use RELATED() to look up values from related dimension tables. Use a calculated column in fact_sales: Customer Region = RELATED(dim_customers[region]).'},
    {id:'pb-m4-s2-i3',difficulty:'medium',
     q:'You need total revenue that responds to a date slicer. Which DAX object is the correct choice?',
     options:['A calculated column: Revenue = fact_sales[revenue]','A measure: Total Revenue = SUM(fact_sales[revenue])','A calculated table: Revenue Table = SUMMARIZE(fact_sales, fact_sales[revenue])','A calculated column using TOTALYTD()'],
     correctIndex:1,explanation:'A measure computes in the current filter context — when a user selects 2024 in the date slicer the filter context restricts fact_sales to 2024 rows and the measure sums only those rows. Calculated columns are fixed at refresh and do not respond to slicer interactions.'},
    {id:'pb-m4-s2-i4',difficulty:'hard',
     q:'On a 10-million-row fact table, a data engineer adds 5 unnecessary calculated columns (revenue_copy, profit_copy, etc.) instead of using measures. What is the performance impact?',
     options:['No impact — calculated columns and measures perform identically','The model stores 50 million additional values (5 columns × 10 million rows) in Vertipaq increasing model file size and memory consumption at refresh and query time','Only refresh performance is affected — query performance is unchanged','The 5 calculated columns cause measures to run slower because Vertipaq prioritises column evaluation'],
     correctIndex:1,explanation:'Each calculated column stores one value per row: 5 columns × 10 million rows = 50 million stored values. This directly increases .pbix file size, memory consumption, and refresh time. Measures have zero storage overhead.'}
  ],
  s3:[ // Row Context vs Filter Context
    {id:'pb-m4-s3-i1',difficulty:'easy',
     q:'In which type of DAX object does row context exist by default?',
     options:['Measures','Quick measures','Calculated columns and iterator functions (SUMX AVERAGEX etc.)','All DAX objects have row context by default'],
     correctIndex:2,explanation:'Row context exists in calculated columns (each row is processed one at a time) and inside iterator functions (SUMX iterates row by row). Measures operate in filter context not row context.'},
    {id:'pb-m4-s3-i2',difficulty:'medium',
     q:'Inside a calculated column in fact_sales, how do you look up the region value from the related dim_customers table?',
     options:['LOOKUPVALUE(dim_customers[region], dim_customers[customer_id], fact_sales[customer_id])','RELATED(dim_customers[region])','RELATEDTABLE(dim_customers)[region]','CALCULATE(dim_customers[region], FILTER(dim_customers, dim_customers[customer_id] = fact_sales[customer_id]))'],
     correctIndex:1,explanation:'Inside a calculated column, RELATED(dim_customers[region]) follows the active relationship from fact_sales to dim_customers for the current row and returns the region value. RELATED works only in row context (calculated columns and iterators).'},
    {id:'pb-m4-s3-i3',difficulty:'medium',
     q:'What is "context transition" in DAX?',
     options:['Moving a measure from one table to another in the Fields pane','When CALCULATE() is called inside a row context (e.g. inside SUMX) it converts the current row context into an equivalent filter context before evaluating the expression','The transition from Import mode to DirectQuery mode at query time','Switching from one filter context to another when a user changes a slicer selection'],
     correctIndex:1,explanation:'Context transition: when CALCULATE() appears inside an iterator\'s row context it converts "I am processing row X" into a filter context "show only rows that match row X\'s key values." This allows measures to be called correctly inside iterators.'},
    {id:'pb-m4-s3-i4',difficulty:'hard',
     q:'A calculated column in fact_sales contains: Profit Per Unit = fact_sales[profit] / fact_sales[quantity]. A measure contains: Avg Profit Per Unit = AVERAGE(fact_sales[Profit Per Unit]). What is the difference in how each computation runs?',
     options:['Both run identically — there is no functional difference','The calculated column evaluates profit/quantity for every row at refresh and stores the result; the measure averages those pre-stored values at query time in filter context','The measure runs faster because it avoids storing data; the calculated column is more accurate because it runs in row context','The calculated column runs in filter context; the measure runs in row context'],
     correctIndex:1,explanation:'Calculated column: row context → computed once per row at refresh → stored. Measure: filter context → AVERAGE runs over only the rows visible in the current filter context, using the pre-stored column values. The stored column exists whether or not the measure uses it.'}
  ],
  s4:[ // CALCULATE
    {id:'pb-m4-s4-i1',difficulty:'easy',
     q:'What does CALCULATE() do in DAX?',
     options:['It performs arithmetic calculations on measure values','It evaluates an expression in a modified filter context — applying the specified filter modifiers before computing the expression','It creates a calculated table from a DAX expression','It calculates the result of all measures on the current page simultaneously'],
     correctIndex:1,explanation:'CALCULATE() is the core context-modification function in DAX. It evaluates its first argument (an expression or measure) after applying all specified filter modifiers — which can add, remove, or replace filters in the current context.'},
    {id:'pb-m4-s4-i2',difficulty:'medium',
     q:'Write the CALCULATE expression that computes total revenue for only the "Electronics" category.',
     options:["CALCULATE(SUM(fact_sales[revenue]), fact_sales[category] = 'Electronics')",'CALCULATE(SUM(fact_sales[revenue]), FILTER(dim_product, dim_product[category] = "Electronics"))','SUMIF(fact_sales[revenue], fact_sales[category], "Electronics")','SUM(fact_sales[revenue]) WHERE fact_sales[category] = "Electronics"'],
     correctIndex:0,explanation:'CALCULATE(SUM(fact_sales[revenue]), fact_sales[category] = "Electronics") adds a filter to the context restricting the category column to "Electronics" before summing revenue. This is the standard direct filter syntax.'},
    {id:'pb-m4-s4-i3',difficulty:'medium',
     q:'What does ALL(dim_customers) do when used as a CALCULATE filter modifier?',
     options:['Adds all dim_customers rows to the filter context','Removes all filters currently applied to dim_customers — the expression evaluates as if no customer filter is active','Duplicates the dim_customers table for use in the calculation','Includes all null values from dim_customers in the result'],
     correctIndex:1,explanation:'ALL(table) removes all filters on that table within the CALCULATE scope. ALL(dim_customers) means "ignore whatever the slicer or visual has selected for customers — compute over all customers." Essential for % of total and benchmark calculations.'},
    {id:'pb-m4-s4-i4',difficulty:'hard',
     q:'What is the difference between ALL() and ALLSELECTED() in a Power BI measure?',
     options:['ALL() removes all filters; ALLSELECTED() removes only row-level filters keeping column-level filters','ALL() removes all filters including outer context slicers; ALLSELECTED() removes internal visual filters but respects the outermost filter context (report-level slicers) — making it suitable for "% of slicer selection" calculations','ALL() works on tables; ALLSELECTED() works only on individual columns','ALLSELECTED() is deprecated — use ALL() with a KEEPFILTERS modifier instead'],
     correctIndex:1,explanation:'ALL() ignores all filters including slicers. ALLSELECTED() removes internal visual context but keeps the slicer\'s outer context. If a user filters to 2024 via a year slicer ALLSELECTED() in a % of total measure gives % within 2024 not % of all years.'}
  ],
  s5:[ // Aggregation & Iterator Functions
    {id:'pb-m4-s5-i1',difficulty:'easy',
     q:'Why is COUNTROWS(fact_sales) preferred over COUNT(fact_sales[order_id]) for counting rows?',
     options:['COUNTROWS is faster because it is a table function not a column function','COUNTROWS counts all rows including those where order_id is null; COUNT skips null values — COUNTROWS is the explicit unambiguous row counter','COUNT is deprecated in Power BI and COUNTROWS replaces it','COUNTROWS works in both row context and filter context; COUNT only works in filter context'],
     correctIndex:1,explanation:'COUNT(column) skips null values — if any order_id is null those rows are not counted. COUNTROWS(table) counts every row regardless of null values in any column. For counting rows use COUNTROWS.'},
    {id:'pb-m4-s5-i2',difficulty:'easy',
     q:'Write a DAX measure that counts the number of unique customers in fact_sales.',
     options:['COUNT(fact_sales[customer_id])','DISTINCTCOUNT(fact_sales[customer_id])','COUNTROWS(DISTINCT(dim_customers))','COUNTUNIQUE(fact_sales[customer_id])'],
     correctIndex:1,explanation:'DISTINCTCOUNT(column) counts the number of distinct non-blank values in a column — the correct function for counting unique customers, products, orders etc.'},
    {id:'pb-m4-s5-i3',difficulty:'medium',
     q:'What is the key difference between SUM(fact_sales[revenue]) and SUMX(fact_sales, fact_sales[revenue])?',
     options:['SUM and SUMX produce identical results in all situations','SUM directly aggregates the column using Vertipaq\'s columnar storage (faster); SUMX iterates row by row evaluating an expression per row before summing (more flexible but slower for simple sums)','SUMX supports DirectQuery mode; SUM does not','SUM only works on integer columns; SUMX works on all numeric types'],
     correctIndex:1,explanation:'SUM(column) is optimised — Vertipaq sums the column directly from its compressed storage. SUMX(table, expression) establishes row context per row and evaluates the expression — necessary when the value to sum requires per-row calculation but slower than SUM for simple cases.'},
    {id:'pb-m4-s5-i4',difficulty:'hard',
     q:'You write: Avg Order Margin = AVERAGEX(fact_sales, DIVIDE(fact_sales[profit], fact_sales[revenue], 0)). Why is this different from DIVIDE(SUM(fact_sales[profit]), SUM(fact_sales[revenue]), 0)?',
     options:['They produce identical results — use whichever you prefer','AVERAGEX computes the margin per order first then averages those per-order margins (average of ratios); DIVIDE(SUM/SUM) computes total profit divided by total revenue (ratio of totals) — these are mathematically different','AVERAGEX is always more accurate because it avoids division by zero','The DIVIDE(SUM/SUM) version works only in Import mode; AVERAGEX works in both Import and DirectQuery'],
     correctIndex:1,explanation:'"Average of ratios" (AVERAGEX) vs "ratio of totals" (DIVIDE SUM/SUM) are different calculations. Example: two orders — one with 50% margin on $100 and one with 10% margin on $1000. AVERAGEX = (50%+10%)/2 = 30%. DIVIDE(SUM/SUM) = ($50+$100)/($100+$1000) = 13.6%. Neither is wrong — they answer different business questions.'}
  ],
  s6:[ // Time Intelligence
    {id:'pb-m4-s6-i1',difficulty:'easy',
     q:'What is the prerequisite for using time intelligence functions like TOTALYTD in DAX?',
     options:['A scheduled refresh must be configured in Power BI Service','A properly marked Date Table in the model — a table with a contiguous range of dates with the date column marked as the key','The date column must be an integer formatted YYYYMMDD not an actual Date type','Time intelligence functions require a Power BI Premium licence'],
     correctIndex:1,explanation:'All time intelligence functions require a marked Date Table. Right-click the date dimension in Model View → Mark as Date Table → select the date column. Without this the functions produce errors or incorrect year boundary calculations.'},
    {id:'pb-m4-s6-i2',difficulty:'easy',
     q:'Write the DAX measure that computes revenue for the same period in the previous year.',
     options:['CALCULATE([Total Revenue], PREVIOUSYEAR(dim_date[date]))','CALCULATE([Total Revenue], SAMEPERIODLASTYEAR(dim_date[date]))','[Total Revenue] - DATEADD([Total Revenue], -1, YEAR)','TOTALYTD([Total Revenue], dim_date[date], LASTDATE(DATEADD(dim_date[date],-1,YEAR)))'],
     correctIndex:1,explanation:'SAMEPERIODLASTYEAR(dim_date[date]) shifts the current filter context back exactly one year. CALCULATE([Total Revenue], SAMEPERIODLASTYEAR(dim_date[date])) returns revenue for the matching period one year ago.'},
    {id:'pb-m4-s6-i3',difficulty:'medium',
     q:'What does TOTALYTD([Total Revenue], dim_date[date]) return when evaluated in a matrix cell showing March 2024?',
     options:['Revenue for March 2024 only','Cumulative revenue from January 1 2024 through the last date in March 2024 visible in the filter context','Revenue for the entire year 2024 (all 12 months)','Revenue from January 1 of all years through March 31'],
     correctIndex:1,explanation:'TOTALYTD accumulates from January 1 (or fiscal year start) of the current year up to the last date in the current filter context. For a matrix cell showing March 2024 it returns Jan + Feb + Mar 2024 cumulative revenue.'},
    {id:'pb-m4-s6-i4',difficulty:'hard',
     q:'Revenue YoY % returns BLANK for January 2022 in a matrix. Why?',
     options:['The DIVIDE function returned 0 not BLANK for missing values','There is no data for January 2021 (prior year) in the dataset — SAMEPERIODLASTYEAR returns BLANK when the prior period has no matching dates in the Date Table','The date dimension does not extend back to 2021','TOTALYTD resets the value to BLANK at the start of each year'],
     correctIndex:1,explanation:'SAMEPERIODLASTYEAR shifts to January 2021. If the dataset or Date Table starts in 2022 there is no prior-year data to calculate. DIVIDE returns BLANK when the denominator is BLANK — correctly showing BLANK for the earliest year\'s YoY comparison.'}
  ],
  s7:[ // Common DAX Patterns
    {id:'pb-m4-s7-i1',difficulty:'medium',
     q:'Write a measure for Revenue % of Total (all customers) in a visual that shows revenue by customer.',
     options:['[Total Revenue] / SUM(fact_sales[revenue])','DIVIDE([Total Revenue], CALCULATE([Total Revenue], ALL(dim_customers)), 0)','DIVIDE([Total Revenue], CALCULATE([Total Revenue], ALLSELECTED(dim_customers)), 0)','[Total Revenue] / TOTALREVENUE'],
     correctIndex:1,explanation:'CALCULATE([Total Revenue], ALL(dim_customers)) removes the customer filter giving company-wide revenue. DIVIDE([Total Revenue] per customer, that total, 0) gives each customer\'s % share.'},
    {id:'pb-m4-s7-i2',difficulty:'medium',
     q:'Why is VAR…RETURN preferred for complex DAX measures?',
     options:['VAR declarations are faster because they use server-side caching','Variables are evaluated once and reused — sub-expressions referenced multiple times are computed only once improving both performance and readability','VAR is required for all measures that use CALCULATE','Variables allow DAX to use parallel computation across multiple CPU cores'],
     correctIndex:1,explanation:'Without VAR a sub-expression referenced multiple times (e.g. [Total Revenue] appearing 3 times in a formula) is recalculated each time. VAR computes it once stores it and reuses the result — faster and clearer.'},
    {id:'pb-m4-s7-i3',difficulty:'hard',
     q:'Write a RANKX measure that ranks customers from highest to lowest revenue (rank 1 = highest).',
     options:['RANK([Total Revenue], ALL(dim_customers[customer_name]), DESC)','RANKX(ALL(dim_customers[customer_name]), [Total Revenue],, DESC, DENSE)','RANKX(dim_customers, [Total Revenue], 1, DESC)','RANK(dim_customers[customer_name], [Total Revenue])'],
     correctIndex:1,explanation:'RANKX(ALL(dim_customers[customer_name]), [Total Revenue],, DESC, DENSE): ALL removes customer filter so all customers are ranked; the measure provides the value to rank on; DESC = highest first; DENSE = no gaps in ranking numbers when values tie.'},
    {id:'pb-m4-s7-i4',difficulty:'hard',
     q:'You write: Cumulative Revenue = CALCULATE([Total Revenue], FILTER(ALL(dim_date[date]), dim_date[date] <= MAX(dim_date[date]))). In a line chart showing month-by-month data what does this return for June?',
     options:['Revenue for June only','Cumulative revenue from the earliest date in the dataset through the last day of June','Revenue for all dates greater than the last day of June','The maximum revenue value across all months up to June'],
     correctIndex:1,explanation:'The FILTER keeps all dates in ALL(dim_date[date]) where the date is less than or equal to the maximum date visible in the current context (end of June). CALCULATE then sums revenue over that expanded date range — producing a running total through June.'}
  ],
  s8:[ // Lab
    {id:'pb-m4-s8-i1',difficulty:'easy',
     q:'In Lab 4 you write Total Revenue = SUM(fact_sales[revenue]). After creating this measure where does it appear in the Power BI Desktop interface?',
     options:['In the Data View grid as a new column in fact_sales','In the Fields pane under the fact_sales table with a calculator icon indicating it is a measure','In the Visualizations pane as an available visual type','In the Power Query Editor as a new transformation step'],
     correctIndex:1,explanation:'Measures appear in the Fields pane under the table they were created in, with a calculator icon (Σ or fx depending on version). They do not appear as columns in Data View.'},
    {id:'pb-m4-s8-i2',difficulty:'medium',
     q:'In the lab, Revenue YoY % returns BLANK for 2022 (the first year of data). Is this correct behaviour?',
     options:['No — BLANK means the DAX formula has an error that must be fixed','Yes — there is no prior year (2021) data to compare against so SAMEPERIODLASTYEAR returns BLANK and DIVIDE returns BLANK correctly','No — it should return 0% for the first year not BLANK','Yes — but you should replace BLANK with 0 using IFERROR for a cleaner visual'],
     correctIndex:1,explanation:'BLANK is the correct result for the first year\'s YoY % — there is genuinely no prior year data to compare. BLANK in a line chart creates a gap which is accurate. Replacing with 0 would falsely imply 0% growth — a data integrity issue.'},
    {id:'pb-m4-s8-i3',difficulty:'hard',
     q:'In the lab Customer Rank measure uses RANKX(ALL(dim_customers[customer_name]), [Total Revenue],, DESC, DENSE). Why is ALL(dim_customers[customer_name]) necessary?',
     options:['ALL is required as the first argument of all RANKX expressions regardless of purpose','Without ALL the current visual filter context restricts dim_customers to the currently selected customer — RANKX would only see one customer and always return rank 1','ALL removes the date filter to ensure ranking is computed across all time periods','ALL(dim_customers[customer_name]) sorts the customer names alphabetically for the ranking'],
     correctIndex:1,explanation:'RANKX needs to see all customers to rank the current customer among them. In a visual showing one customer at a time the filter context restricts to that customer. ALL() removes that restriction allowing RANKX to evaluate Total Revenue for all customers and correctly determine the rank of the current one.'}
  ]
},

// ══════════════════════════════════════════════════════
// MODULE 5 — Visualizations & Interactive Reports  (15%)
// ══════════════════════════════════════════════════════
m5:{
  s1:[ // Chart Types
    {id:'pb-m5-s1-i1',difficulty:'easy',
     q:'You need to show revenue trends over 24 consecutive months. Which chart type is most appropriate?',
     options:['Clustered Bar Chart','Pie Chart','Line Chart','Scatter Chart'],
     correctIndex:2,explanation:'A Line Chart is the correct choice for showing continuous trends over time. The connected line makes the trend direction immediately visible. Bar charts are for discrete category comparisons not continuous time series.'},
    {id:'pb-m5-s1-i2',difficulty:'easy',
     q:'Which Power BI visual displays a single number prominently — ideal for Total Revenue or Order Count headlines?',
     options:['KPI visual','Card visual','Gauge visual','Matrix visual'],
     correctIndex:1,explanation:'The Card visual displays one scalar measure as a large number — perfect for dashboard headlines showing key totals at a glance.'},
    {id:'pb-m5-s1-i3',difficulty:'medium',
     q:'When should you prefer a Matrix visual over a Table visual in Power BI?',
     options:['When you need to show more than 10 columns','When you need a cross-tabulation with row and column groupings and subtotals — Matrix supports row/column hierarchies and drill-down; Table shows flat rows only','When performance is critical — Matrix queries run faster','When RLS is applied — Matrix handles row security better'],
     correctIndex:1,explanation:'Matrix is a pivot-table-like visual supporting two-dimensional grouping (rows and columns) with subtotals, grand totals, and drill-down hierarchies. Table shows flat rows with no grouping — use it for line-item detail views.'},
    {id:'pb-m5-s1-i4',difficulty:'hard',
     q:'A KPI visual requires three fields. Which three?',
     options:['A dimension field a measure field and a date field','A Value measure a Target measure and a Date/Time axis for trend direction','A current period measure a prior period measure and a variance measure','A measure a table and a filter field'],
     correctIndex:1,explanation:'The KPI visual needs: (1) Value — the current measure to display; (2) Target — a goal or benchmark measure; (3) Trend Axis — a date/time field that determines the trend sparkline direction. Without all three the KPI cannot show goal attainment and trend.'}
  ],
  s2:[ // Slicers, Filters, Cross-filter
    {id:'pb-m5-s2-i1',difficulty:'easy',
     q:'A filter applied in the Filters pane at "page" level affects which visuals?',
     options:['Only the visual that was selected when the filter was applied','All visuals on the current report page only','All visuals on all pages of the report','Only visuals that explicitly reference the filtered field in their data wells'],
     correctIndex:1,explanation:'Page-level filters apply to all visuals on that specific page. Report-level filters apply to all pages. Visual-level filters apply to only the selected visual.'},
    {id:'pb-m5-s2-i2',difficulty:'medium',
     q:'What is the difference between cross-filter mode "Filter" and "Highlight" when clicking a bar in a source visual?',
     options:['Filter and Highlight produce identical results','Filter removes non-matching data from target visuals entirely; Highlight keeps all data visible but dims non-matching marks leaving matching marks fully coloured','Highlight removes data; Filter dims non-matching marks','Filter applies only to measure values; Highlight applies to dimension labels'],
     correctIndex:1,explanation:'Filter: target visuals only show matching rows — non-matching data disappears. Highlight: all data remains visible in the target visual but non-matching marks are dimmed and matching marks are fully opaque.'},
    {id:'pb-m5-s2-i3',difficulty:'medium',
     q:'Which slicer type is most appropriate for filtering data to "the last 30 days" relative to today?',
     options:['List slicer with a text search box','Between slicer with manually set date boundaries','Relative Date slicer set to "Last 30 Days"','Dropdown slicer with all available dates'],
     correctIndex:2,explanation:'Relative Date slicer types ("Last N days/weeks/months") automatically update relative to the current date — always showing the most recent period without manual boundary adjustments. The Between slicer requires manual date entry and would become outdated.'},
    {id:'pb-m5-s2-i4',difficulty:'hard',
     q:'A user selects two values in a slicer (North and South). By default how does Power BI handle this multi-select in DAX measures?',
     options:['Only the first selected value (North) is applied — multi-select is not supported by default','Both North and South are applied as OR conditions in the filter context — the measure computes over rows where region is North OR South','Both North and South are applied as AND conditions — only rows matching both are included','Multi-select triggers an ALLSELECTED() evaluation automatically'],
     correctIndex:1,explanation:'Multi-select in slicers creates an OR filter in the filter context — the measure sees rows where region IN ("North", "South"). The DAX filter context contains multiple values for the sliced column not AND logic.'}
  ],
  s3:[ // Drill-Through Drill-Down Tooltips
    {id:'pb-m5-s3-i1',difficulty:'easy',
     q:'What is the difference between drill-down and drill-through in Power BI?',
     options:['Drill-down navigates to another report file; drill-through navigates within the same report','Drill-down expands a hierarchy within the same visual (Year → Quarter → Month); drill-through navigates to a separate detail page filtered by the selected data point','Drill-down uses bookmarks; drill-through uses page navigation buttons','They are identical features with different names in different Power BI versions'],
     correctIndex:1,explanation:'Drill-down: within a single visual with a hierarchy — clicking expands to the next level (Year to Month). Drill-through: right-click a data point → navigate to a separate report page pre-filtered to that selection.'},
    {id:'pb-m5-s3-i2',difficulty:'medium',
     q:'How do you configure a report page as a drill-through destination?',
     options:['Right-click the page tab → Enable Drillthrough','On the target page drag the field you want to filter by into the "Drillthrough" well in the Visualizations pane','In Page settings enable "Allow drill-through" and specify the source field','In the source visual Format pane enable "Drill-through target" and select the destination page'],
     correctIndex:1,explanation:'On the drill-through destination page drag the field to filter on (e.g. customer_name) into the Drillthrough well. This tells Power BI which field triggers navigation to this page and what filter to apply when it arrives.'},
    {id:'pb-m5-s3-i3',difficulty:'medium',
     q:'What setting must be enabled on a report page for it to function as a custom tooltip that appears on hover?',
     options:['Page settings → "Enable hover tooltips"','Format pane → Page Information → toggle on "Allow use as tooltip"','Visual Format pane → Tooltip → Type: Report Page','Insert → Tooltip → Assign to Current Page'],
     correctIndex:1,explanation:'In the Format pane of the page (click on blank canvas) → Page information → enable "Allow use as tooltip". This marks the page as a tooltip page. Then set the page size to Tooltip and build the hover content on it.'},
    {id:'pb-m5-s3-i4',difficulty:'hard',
     q:'After right-clicking a Treemap slice to drill through to a customer detail page, what filter is automatically applied on the detail page?',
     options:['All report-level filters plus the drillthrough field value from the selected slice','Only the drillthrough field value — all other filters are cleared on arrival','The page filters of the source page are duplicated on the detail page','No filter is applied — the user must manually select a filter on the detail page'],
     correctIndex:0,explanation:'By default drill-through passes the filter context of the selected data point. With "Keep all filters" enabled (Format pane on the detail page) all active report-level filters from the source page are also passed through — not just the drillthrough field value.'}
  ],
  s4:[ // Bookmarks & Navigation
    {id:'pb-m5-s4-i1',difficulty:'easy',
     q:'What does a Power BI bookmark capture by default?',
     options:['Only the current page\'s visual formatting','The current filter and slicer state visible/hidden objects (from Selection pane) and drill state of visuals on the page','The entire report\'s state across all pages','Only which visual was last clicked by the user'],
     correctIndex:1,explanation:'A bookmark captures: (1) filter and slicer state; (2) which objects are visible/hidden in the Selection pane; (3) the drill state of visuals. These three elements define the full "state" a bookmark saves and restores.'},
    {id:'pb-m5-s4-i2',difficulty:'medium',
     q:'How would you create a button that toggles between two different chart types (Bar Chart and Line Chart) at the same position on the canvas?',
     options:['Create two separate report pages and link them with page navigation buttons','Place both charts at the same canvas position → use Selection pane to show one and hide the other → create two bookmarks (one per visible chart state) → assign each bookmark to a button','Use the Visual Switch feature in Format → Visual type','Create a parameter that switches between visual types'],
     correctIndex:1,explanation:'The correct pattern: place both charts in the same canvas area → Selection pane: save "Bookmark A" with Chart A visible and Chart B hidden; save "Bookmark B" with Chart B visible and Chart A hidden → create two buttons each assigned to the respective bookmark.'},
    {id:'pb-m5-s4-i3',difficulty:'medium',
     q:'What does the Bookmark Navigator visual automate?',
     options:['It automatically creates bookmarks from all slicer states in the report','It generates a button for each bookmark in the report — creating a navigation bar without manually assigning bookmarks to individual buttons','It navigates to bookmarks based on the user\'s role (RLS-aware navigation)','It syncs bookmarks across multiple reports in the same workspace'],
     correctIndex:1,explanation:'Insert → Buttons → Navigator → Bookmark Navigator automatically creates one button per bookmark in the report. It handles the bookmark assignment automatically — no manual configuration per button needed.'},
    {id:'pb-m5-s4-i4',difficulty:'hard',
     q:'You create a "Reset Filters" bookmark with all slicers cleared. Later you add a new slicer to the page. Does the existing bookmark also reset this new slicer?',
     options:['Yes — bookmarks automatically capture all future slicers on the page','No — the bookmark captured the state at creation time. The new slicer was not present then. Update the bookmark (three-dot menu → Update) after clearing the new slicer to include it','Yes — but only if the new slicer uses a field that was already in the model when the bookmark was created','No — you must delete the old bookmark and create a new one from scratch'],
     correctIndex:1,explanation:'Bookmarks capture state at creation time. A new slicer added after the bookmark was created is not included in the old bookmark\'s scope. Right-click the bookmark → Update to refresh it with the current page state including the new slicer.'}
  ],
  s5:[ // Report Design Best Practices
    {id:'pb-m5-s5-i1',difficulty:'easy',
     q:'What tool in Power BI Desktop helps you identify which visuals are causing slow page load times?',
     options:['Performance Analyzer (View → Performance Analyzer)','Query Diagnostics in Power Query','DAX Studio connected to the model','Visual Performance in Format pane → Advanced'],
     correctIndex:0,explanation:'Performance Analyzer (View → Performance Analyzer → Start Recording → interact with the report) measures the DAX query time rendering time and other durations for each visual — revealing which visuals are the bottleneck.'},
    {id:'pb-m5-s5-i2',difficulty:'medium',
     q:'What is the purpose of the Mobile Layout view in Power BI Desktop?',
     options:['To preview how the report looks on a 13-inch laptop screen','To design an alternative vertical layout optimised for phone-sized screens that is shown automatically in the Power BI mobile app','To reduce the report file size for faster loading on mobile networks','To apply touch-friendly themes with larger buttons for tablet use'],
     correctIndex:1,explanation:'Mobile Layout (View → Mobile Layout) lets you arrange selected visuals in a vertical phone-friendly layout. When viewed in the Power BI mobile app this layout is shown automatically instead of the desktop layout.'},
    {id:'pb-m5-s5-i3',difficulty:'medium',
     q:'Why should slicers be placed consistently (left or top) on every report page?',
     options:['Power BI requires slicers in specific locations for cross-page sync to work','Consistent placement reduces cognitive load — users know immediately where to find filter controls without hunting across pages','Slicers only cross-filter correctly when placed in the left or top margin','Performance is better when slicers are outside the main chart area'],
     correctIndex:1,explanation:'Users develop a mental model of the report layout. Consistent slicer placement (always left column or always top strip) means users immediately know where to look without re-scanning every page to find filter controls.'},
    {id:'pb-m5-s5-i4',difficulty:'hard',
     q:'A Power BI report page has 20 visuals. Users report slow performance. What is the first optimization to consider?',
     options:['Switch all data connections from Import to DirectQuery','Reduce the number of visuals on the page — each visual generates one or more DAX queries which execute simultaneously when the page loads','Increase the Power BI Service capacity SKU','Apply a report theme which reduces the visual rendering time'],
     correctIndex:1,explanation:'Each visual sends one or more DAX queries to the engine when the page loads. 20 visuals may generate 20-40 simultaneous queries — overloading the engine. The first step is to reduce visuals per page consolidating information into fewer well-designed charts.'}
  ],
  s6:[ // Lab
    {id:'pb-m5-s6-i1',difficulty:'easy',
     q:'In Lab 5 you configure a drill-through from Page 2 (Category Deep Dive) to Page 3 (Customer Detail). A user right-clicks a Treemap slice for Electronics and selects Drill through → Customer Detail. What data does Page 3 show?',
     options:['All customer data unfiltered','Customer data filtered to show only customers who purchased Electronics','Customer data filtered to the category clicked plus any active page-level slicer selections','The top 10 customers by revenue regardless of the clicked category'],
     correctIndex:1,explanation:'Drill-through passes the filter context of the selected data point. Clicking Electronics passes category = "Electronics" to Page 3 — showing only Electronics-related customer data. With "Keep all filters" enabled additional active slicer context also passes through.'},
    {id:'pb-m5-s6-i2',difficulty:'medium',
     q:'In Lab 5 after building the tooltip page and assigning it to the Page 1 bar chart you hover over the "North" bar. What appears?',
     options:['The default Power BI tooltip showing the field values of that bar','The custom tooltip page content (Card visuals for Total Revenue and Profit Margin) filtered to North region data','A navigation button linking to the Page 2 drill-through page','An error because tooltip pages do not support filtering by the hovered mark'],
     correctIndex:1,explanation:'Custom tooltip pages appear in a floating panel when hovering over a mark. The page inherits the filter context of the hovered mark (region = North) so all visuals on the tooltip page show North-filtered data.'},
    {id:'pb-m5-s6-i3',difficulty:'hard',
     q:'In Lab 5 you create a "Reset Filters" bookmark with all slicers cleared and attach it to a button. When a user clicks Reset Filters what exactly happens?',
     options:['All slicers on all pages are cleared simultaneously','The report navigates to the page where the bookmark was created and restores all captured state (cleared slicers visible objects drill level) for that page','Only measure calculations are reset — slicer positions are not affected','The button reloads the dataset from the source to restore the original data state'],
     correctIndex:1,explanation:'Clicking the button triggers the bookmark which restores the exact state captured: cleared slicers on this page visible/hidden objects and drill state. Other pages are not affected — bookmarks are page-scoped unless configured as cross-page navigation bookmarks.'}
  ]
},

// ══════════════════════════════════════════════════════
// MODULE 6 — Workspaces, Apps & Row Level Security  (15%)
// ══════════════════════════════════════════════════════
m6:{
  s1:[ // Service & Workspaces
    {id:'pb-m6-s1-i1',difficulty:'easy',
     q:'Which workspace role can add or remove other members but cannot change Admin-level roles or delete the workspace?',
     options:['Viewer','Contributor','Member','Owner'],
     correctIndex:2,explanation:'Member role can add/remove members (except Admins), publish and update apps, and edit content. Members cannot change Admin roles or delete the workspace — those actions require Admin access.'},
    {id:'pb-m6-s1-i2',difficulty:'easy',
     q:'Why is My Workspace not suitable for sharing team content?',
     options:['My Workspace has a 100 MB file size limit','My Workspace content cannot be shared with other users — it is a private personal space only the owner can access','My Workspace does not support scheduled refresh','My Workspace cannot store reports with more than 5 pages'],
     correctIndex:1,explanation:'My Workspace is personal and private. You cannot add other members to My Workspace or share its content directly (you can share individual report links but not give collaborative workspace access). All team content belongs in a shared workspace.'},
    {id:'pb-m6-s1-i3',difficulty:'medium',
     q:'What licence is required to publish content to a shared workspace and collaborate with others?',
     options:['Power BI Free (no licence required)','Power BI Pro or Premium Per User (PPU)','Microsoft 365 E3 or higher','Power BI Embedded A-SKU licence'],
     correctIndex:1,explanation:'A Power BI Pro licence (or PPU) is required to publish to shared workspaces create apps and share content with other Pro users. Free users are limited to My Workspace only.'},
    {id:'pb-m6-s1-i4',difficulty:'hard',
     q:'A workspace has Admin Member Contributor and Viewer roles. Which role is appropriate for an end user who should only view published reports and dashboards without any editing capability?',
     options:['Contributor — they can create content but not edit others\'','Member — they have broad collaborative access','Viewer — read-only access to all workspace content','No role is needed — share individual report links instead of adding them to the workspace'],
     correctIndex:2,explanation:'Viewer role: read-only access to all workspace content. Viewers cannot edit publish or manage any content. For most end users distributing an App (not direct workspace access) is preferable — Apps provide a cleaner consumer experience without exposing workspace content directly.'}
  ],
  s2:[ // Publishing & Sharing
    {id:'pb-m6-s2-i1',difficulty:'easy',
     q:'When you publish from Power BI Desktop to the Service what two objects are created?',
     options:['A report and a dashboard','A dataset and a report','A workspace and a dataset','A dataflow and a dataset'],
     correctIndex:1,explanation:'Publishing creates a dataset (the data model with tables relationships measures and Import data) and a report (the visual layout with pages and charts). These are separate independently manageable objects.'},
    {id:'pb-m6-s2-i2',difficulty:'medium',
     q:'Which sharing method makes a Power BI report publicly accessible to anyone on the internet with no authentication?',
     options:['Share a report link (requires Pro licence)','Embed in SharePoint Online (requires SharePoint access)','Publish to Web (creates a public iframe embed accessible to anyone)','Distribute via a Power BI App (requires app audience membership)'],
     correctIndex:2,explanation:'Publish to Web creates a public embed code — anyone with the URL can view the report with no sign-in. Use only for genuinely public data. Never use for internal, confidential, or personally identifiable data.'},
    {id:'pb-m6-s2-i3',difficulty:'medium',
     q:'What is required to refresh a dataset built from a local on-premises SQL Server database via scheduled refresh in Power BI Service?',
     options:['A Power BI Premium licence','An On-Premises Data Gateway installed on a machine with access to the SQL Server','The SQL Server must be migrated to Azure SQL first','A DirectQuery connection — Import mode does not support gateway refresh'],
     correctIndex:1,explanation:'An On-Premises Data Gateway acts as a secure bridge between Power BI Service (cloud) and on-premises data sources. It must be installed on a machine that can reach the SQL Server and configured in Service settings.'},
    {id:'pb-m6-s2-i4',difficulty:'hard',
     q:'You republish a report from Desktop (same name same workspace). What happens to the existing dataset and report in the Service?',
     options:['A new separate report and dataset are created with an incremented version number','The existing report is replaced but the dataset is preserved with all its settings (scheduled refresh credentials favourites etc.)','Both the existing dataset and report are replaced — all Service-level settings on the dataset may need to be reconfigured','Only the report layout is updated — the dataset is never touched by republishing'],
     correctIndex:1,explanation:'Republishing overwrites the report layout (visual changes from Desktop). The dataset is also overwritten (model changes) but Power BI attempts to preserve Service-side settings like scheduled refresh schedules and credentials. Always verify gateway and refresh settings after republishing a model with structural changes.'}
  ],
  s3:[ // Power BI Apps
    {id:'pb-m6-s3-i1',difficulty:'easy',
     q:'When you update a report in the workspace does the Power BI App update automatically for end users?',
     options:['Yes — workspace changes are reflected in the App immediately','No — you must explicitly click "Update app" to push workspace changes to the App audience','Yes — but only if the dataset was also refreshed since the last App publication','No — changes require publishing an entirely new App with a new name'],
     correctIndex:1,explanation:'Workspace content and the published App are independent. Editing a report in the workspace does not update the App. You must explicitly click "Update app" to push the new version to your audience — allowing controlled release cycles.'},
    {id:'pb-m6-s3-i2',difficulty:'medium',
     q:'What is the recommended distribution method for sharing Power BI content with 500 end users across the organisation?',
     options:['Share individual report links with all 500 users via email','Give all 500 users Contributor access to the workspace','Publish a Power BI App and add the 500 users (or their security group) to the App audience','Export reports as PDF and email them quarterly'],
     correctIndex:2,explanation:'Apps are the correct large-scale distribution mechanism. Create the App from the workspace, add the users/security group to the audience tab, and publish. Users get a curated stable view without workspace access — and you can update without re-sharing links.'},
    {id:'pb-m6-s3-i3',difficulty:'medium',
     q:'In the App publishing flow on which tab do you specify who can access the App?',
     options:['Setup tab','Content tab','Permissions tab','Audience tab'],
     correctIndex:3,explanation:'The Audience tab (third step in the app publishing wizard) is where you define who can access the App — specific users by email, security groups, or the entire organisation.'},
    {id:'pb-m6-s3-i4',difficulty:'hard',
     q:'A Power BI Premium workspace allows multiple App audiences with different content visibility. What does this enable?',
     options:['Different users see the same reports but with different themes applied','Different user groups (Sales Finance HR) can each receive a tailored App navigation showing only the reports relevant to them from the same workspace','Multiple App audiences allow concurrent editing by different teams simultaneously','Premium multi-audience Apps bypass the Power BI Pro licence requirement for all viewers'],
     correctIndex:1,explanation:'Premium multi-audience Apps let you define different report subsets for different audiences from one workspace. Sales audience sees Sales Dashboard + Regional Reports. Finance audience sees Financial Reports + Budget Model. Same workspace different curated App experiences.'}
  ],
  s4:[ // Introduction to RLS
    {id:'pb-m6-s4-i1',difficulty:'easy',
     q:'What is Row Level Security (RLS) in Power BI?',
     options:['A feature that encrypts data rows at rest in the Vertipaq engine','A mechanism that restricts which data rows a specific user sees when they open a report — based on their identity or assigned role','A setting that prevents users from exporting rows to Excel from a visual','A Power BI Premium feature that limits query row counts for performance management'],
     correctIndex:1,explanation:'RLS filters the data model at query time before any visuals render. Users see only the data rows they are authorised to see — the restriction is invisible to them. One report serves many audiences with each user seeing only their permitted data.'},
    {id:'pb-m6-s4-i2',difficulty:'medium',
     q:'Which workspace roles are exempt from RLS filters?',
     options:['All workspace roles are subject to RLS — no one is exempt','Workspace Admins and Members — they bypass RLS and see all data regardless of role assignment','Workspace Viewers and external users — RLS does not apply to them','Only users with a Power BI Premium licence are exempt from RLS'],
     correctIndex:1,explanation:'Workspace Admins and Members are exempt from RLS — they see all data regardless of which roles they are assigned to. Only workspace Viewers (and users who access content through Apps without workspace access) are restricted by RLS.'},
    {id:'pb-m6-s4-i3',difficulty:'medium',
     q:'What is the difference between static and dynamic RLS?',
     options:['Static RLS uses DAX; dynamic RLS uses M language filters in Power Query','Static RLS has a fixed filter that shows the same data to everyone in that role (e.g. region = "North"); dynamic RLS uses USERPRINCIPALNAME() to adapt the filter to each user\'s identity','Static RLS requires more maintenance; dynamic RLS is applied automatically by Power BI without any configuration','Dynamic RLS requires a Power BI Premium licence; static RLS is available on Pro'],
     correctIndex:1,explanation:'Static: one fixed filter per role — e.g. NorthRegionRole shows only North data to all members. Dynamic: one role uses USERPRINCIPALNAME() so each user\'s email determines what they see — one role handles all users adaptively via a permissions mapping table.'},
    {id:'pb-m6-s4-i4',difficulty:'hard',
     q:'Where in Power BI Service do you assign users to RLS roles?',
     options:['In the report\'s Share settings → Role Assignment tab','On the dataset → three-dot menu → Security → select role → add user email or security group','In the workspace → Settings → Row Level Security → assign users','In Power BI Desktop → Manage Roles → Assign Users tab before publishing'],
     correctIndex:1,explanation:'RLS user assignment happens on the dataset in the Service (not in Desktop and not on the report). Navigate to the dataset in the workspace → three-dot menu → Security → select a role → add users or security groups by email.'}
  ],
  s5:[ // Implementing RLS via DAX
    {id:'pb-m6-s5-i1',difficulty:'easy',
     q:'Write the DAX rule expression for a dynamic RLS role that restricts a user_permissions table to show only the current logged-in user\'s rows.',
     options:['[user_email] = CURRENTUSER()','[user_email] = USERPRINCIPALNAME()','user_permissions[user_email] = USERNAME()','FILTER(user_permissions, [user_email] = LOGINNAME())'],
     correctIndex:1,explanation:'USERPRINCIPALNAME() returns the UPN (email) of the currently authenticated user. The DAX rule [user_email] = USERPRINCIPALNAME() filters the user_permissions table to show only the row(s) matching the logged-in user\'s email.'},
    {id:'pb-m6-s5-i2',difficulty:'medium',
     q:'After defining RLS roles in Power BI Desktop and publishing to Service, what additional step is required for RLS to be active for end users?',
     options:['Enable RLS in the report Format pane → Security settings','Assign users or security groups to the roles in the Service — navigate to the dataset → Security → select role → add users','Republish the report with RLS mode enabled in the publish dialog','Create a Power BI App — RLS is only enforced for App users not direct report viewers'],
     correctIndex:1,explanation:'Defining roles in Desktop creates the role definitions. Assigning users in the Service activates those roles for specific people. Without assignment no users are restricted — everyone sees all data.'},
    {id:'pb-m6-s5-i3',difficulty:'medium',
     q:'How do you test that an RLS role is working correctly after defining it in Power BI Desktop?',
     options:['Publish the report and log in as a test user with a different account','Modeling tab → View as → select the role and optionally enter a test user email — the report canvas updates to show filtered data','Create a test page with a visual showing all data and compare it to the filtered visual','Run a DAX query using EVALUATE TABLE(FILTER(fact_sales, USERPRINCIPALNAME()=...)) in the query bar'],
     correctIndex:1,explanation:'Modeling → View as role: Desktop simulates the filter context of the selected role — you see the report exactly as a user in that role would. For dynamic roles enter a specific user email to simulate that user\'s filter. This is the fastest way to verify before publishing.'},
    {id:'pb-m6-s5-i4',difficulty:'hard',
     q:'In dynamic RLS, the user_permissions table (user_email → region) is related to dim_customers (region → region) with bidirectional cross-filter. Why is bidirectional required here?',
     options:['Bidirectional is always required for any RLS relationship to function','The USERPRINCIPALNAME() filter restricts user_permissions. For that filter to propagate to dim_customers (and then to fact_sales via the standard 1:M relationship) filters must flow from user_permissions TO dim_customers — which requires bidirectional on that specific relationship','Bidirectional is only required for Many-to-Many cardinality relationships','USERPRINCIPALNAME() can only evaluate correctly in bidirectional cross-filter models'],
     correctIndex:1,explanation:'Standard single-direction filtering flows from dim to fact. The RLS filter starts at user_permissions and needs to flow TO dim_customers (an unusual direction for RLS). Bidirectional on the user_permissions → dim_customers relationship allows the filter to propagate in both directions — enabling the RLS filter to reach dim_customers and then fact_sales via the normal 1:M path.'}
  ],
  s6:[ // Lab
    {id:'pb-m6-s6-i1',difficulty:'easy',
     q:'In Lab 6 you create a "North" static role with filter [region] = "North" applied to dim_customers. After clicking View as → North what should you observe on the report\'s bar chart?',
     options:['All regions are shown with North highlighted in a different colour','Only the North region bar is visible — South East and West data is filtered out','The bar chart shows a warning that RLS is active but all data remains visible','The total revenue value doubles because RLS applies the filter twice'],
     correctIndex:1,explanation:'The RLS filter restricts dim_customers to only North rows. The filter propagates through the dim_customers → fact_sales relationship. Only fact_sales rows linked to North customers are visible — so the bar chart shows only North revenue.'},
    {id:'pb-m6-s6-i2',difficulty:'medium',
     q:'In Lab 6 Part B you create a user_permissions table and use USERPRINCIPALNAME() in the dynamic role. When you test with View as → Other user: alice@sourcefuse.com what data appears?',
     options:['All data — View as Other User bypasses RLS filters','Only data for alice\'s assigned region (North) because USERPRINCIPALNAME() evaluates to alice@sourcefuse.com and user_permissions maps that email to North','Data for all regions because alice@sourcefuse.com is in the Admin group','No data — USERPRINCIPALNAME() does not work in View as mode'],
     correctIndex:1,explanation:'View as → Other User simulates USERPRINCIPALNAME() returning alice@sourcefuse.com. The RLS DAX filter [user_email] = USERPRINCIPALNAME() resolves to [user_email] = "alice@sourcefuse.com". user_permissions returns only the North row for Alice — which then filters the model to North data only.'},
    {id:'pb-m6-s6-i3',difficulty:'hard',
     q:'After publishing the Lab 6 model and assigning a colleague to the "North" static role in the Service, the colleague reports they can still see all regional data. What is the most likely cause?',
     options:['Static RLS does not work after publishing — only dynamic RLS is active in the Service','The colleague has Workspace Admin or Member access — these roles bypass RLS regardless of role assignment','The North filter was incorrectly written — [region] = "North" should be [region] == "North"','RLS role assignments require 24 hours to propagate in the Service'],
     correctIndex:1,explanation:'Workspace Admins and Members bypass RLS entirely — they always see all data. If your colleague is a workspace Admin or Member the North role assignment has no effect. Assign them as a Viewer in the workspace (or give access only via the App) for RLS to apply.'}
  ]
}

}; // end window.PB_QUESTION_BANK
